@{
    ViewBag.Title = "Add";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@model VisualSmart.Domain.SetUp.UserDomain
@using (Html.BeginForm())
{
    <div class="container-fluid">
        <div class="table-responsive panel panel-primary">
            <div class="panel-heading" role="tab" id="headingOne">
                <div class="panel-title">
                    <strong>系统用户-添加</strong>
                </div>
            </div>
            @*<div class="alert alert-danger">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    <strong>用户名已经存在！</strong>
                </div>*@
            <table class="table table-condensed panel-body">
                <tr>
                    <td><label for="loginId"><span style="color:red;">*</span>用户名:</label></td>
                    <td>@Html.TextBoxFor(t => t.loginId, new { @class = "form-control", @data_container = "body", @data_toggle = "popover", @data_placement = "bottom", @data_content = "请输入用户名", @data_trigger = "focus" })</td>
                </tr>
                <tr>
                    <td><label for="loginPwd"><span style="color:red;">*</span>登录密码:</label></td>
                    <td>@Html.TextBoxFor(t => t.loginPwd, new { @class = "form-control", @data_container = "body", @data_toggle = "popover", @data_placement = "bottom", @data_content = "请输入登录密码", @data_trigger = "focus" })</td>
                </tr>
                <tr>
                    <td><label for="Name"><span style="color:red;">*</span>真实姓名:</label></td>
                    <td>@Html.TextBoxFor(t => t.Name, new { @class = "form-control", @data_container = "body", @data_toggle = "popover", @data_placement = "bottom", @data_content = "请输入真实姓名", @data_trigger = "focus" })</td>
                </tr>
                <tr>
                    <td><label for="Phone">手机:</label></td>
                    <td>@Html.TextBoxFor(t => t.Phone, new { @class = "form-control" })</td>
                </tr>
                <tr>
                    <td><label for="Email">Email:</label></td>
                    <td>@Html.TextBoxFor(t => t.Email, new { @class = "form-control" })</td>
                </tr>
                <tr>
                    <td><label for="Position">职位:</label></td>
                    <td>@Html.TextBoxFor(t => t.Position, new { @class = "form-control" })</td>
                </tr>
                <tr>
                    <td><label for="Leader">所属上级:</label></td>
                    <td>@Html.TextBoxFor(t => t.Leader, new { @class = "form-control" })</td>
                </tr>
                <tr>
                    <td><label for="Company">所属公司:</label></td>
                    <td>@Html.TextBoxFor(t => t.Company, new { @class = "form-control" })</td>
                </tr>
            </table>

        </div>
        <div class="form-inline panel-body" style="margin-left: auto;margin-right: auto;">
            <div class="form-group">
                <input type="submit" class="btn btn-primary btn-block" value="添加" id="btnSave" />
            </div>
            <div class="form-group">
                <input type="reset" class="btn btn-default btn-block" value="重置" />
            </div>
            <div class="form-group">
                <a class="btn btn-default btn-block" href="/User/Index">返回</a>
            </div>
        </div>

    </div>
    <script>
        $(function () {
            $('[data-toggle="popover"]').popover();
        })

        $("#btnSave").click(function () {
            return CheckValue("loginId");
        });

        function CheckValue(name) {
            var control = $("#" + name + "");
            if (control.val() == "") {
                control.focus();
                return false;
            }
            return tru;
        }
    </script>
}